# Fiorwoods runtime sprites

This directory contains only the Fiorwoods sprites referenced by the production game.
The complete locally licensed library remains under the gitignored
`assets/chained-echoes-assets-sorted` directory for the style editor.

Run `npm run sync:runtime-assets --workspace @labyrinth/client` after changing the runtime
sprite IDs in the client.
